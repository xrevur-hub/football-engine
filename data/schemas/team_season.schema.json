{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://football-engine.local/schemas/team_season.schema.json",
  "title": "TeamSeasonDataset",
  "description": "On-disk shape of data/normalized/teams/team_seasons.json. AUTHORITY NOTE: hand-maintained mirror of football_engine.data_layer.schemas.TeamSeasonDataset/TeamSeasonRecord for external tooling only — Pydantic (schemas.py) is authoritative at load time.",
  "type": "object",
  "required": ["metadata", "teams"],
  "properties": {
    "metadata": { "$ref": "player_season.schema.json#/$defs/DatasetMetadata" },
    "teams": {
      "type": "array",
      "items": { "$ref": "#/$defs/TeamSeasonRecord" }
    }
  },
  "$defs": {
    "TeamSeasonRecord": {
      "type": "object",
      "required": ["id", "club", "season", "roster", "metadata"],
      "properties": {
        "id": { "type": "string", "minLength": 1, "example": "barcelona_2010_11" },
        "club": { "type": "string", "minLength": 1 },
        "season": { "type": "string", "pattern": "^[0-9]{4}/[0-9]{2}$", "example": "2010/11" },
        "roster": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" },
          "description": "PlayerSeason id references — must all exist in player_seasons.json. No duplicates allowed."
        },
        "default_formation": {
          "type": ["string", "null"],
          "description": "Optional Formation.name reference (e.g. \"4-3-3\"), validated against formations.json when a FormationRepository is supplied to the loader."
        },
        "metadata": { "$ref": "player_season.schema.json#/$defs/SourceMetadata" }
      }
    }
  }
}
