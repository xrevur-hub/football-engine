# Preserved source references

The two .original.md files are byte-for-byte copies of user attachments, not automatically current implementation instructions. Their hashes are recorded in the V3 verification audit.

Priority for this delivery: explicit current user request and finalized E-12/Formation/Layer 3 decisions; corrected V3 memo for its specified Module 2/3 implementation scope; original checkpoint and V2 memo as historical/reference evidence. Legacy formation_type/Type A/B/C structural passages do not override E-12. Missing V1 sections mentioned in V2 were not silently reconstructed as facts.
